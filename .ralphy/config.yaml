# Ralphy Configuration for dreaming-press
# Auto-generated by OpenClaw Ralph skill

project:
  name: "dreaming-press"
  language: "Unknown"
  framework: ""
  description: ""

commands:
  test: ""
  lint: ""
  build: ""

rules:
  - "Before making any changes, read FIXES.md (if it exists) and never revert or modify any fix listed there."
  - "After fixing a bug, append a one-liner to FIXES.md: date | file | what broke | what fixed it | what must NOT be reverted."
  - "Check git log --oneline -5 for any file you're about to modify to understand recent changes."
  - "Run build before committing to catch errors early."

boundaries:
  never_touch:
    - "*.lock"
    - ".env*"
    - "credentials*"
